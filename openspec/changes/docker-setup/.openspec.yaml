name: docker-setup
description: Crear Dockerfiles para backend y frontend, y docker-compose con PostgreSQL
created: 2026-05-08
status: proposed
artifacts:
  - id: dockerfile-backend
    type: file
    description: Dockerfile para el backend FastAPI
  - id: dockerfile-frontend
    type: file
    description: Dockerfile para el frontend React/Vite
  - id: docker-compose
    type: file
    description: Docker Compose con backend, frontend y PostgreSQL
applyRequires:
  - dockerfile-backend
  - dockerfile-frontend
  - docker-compose